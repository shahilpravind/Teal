#pragma once

#include <cstdint>
#include <string>

const std::string WINDOW_TITLE = "Teal";
const uint16_t WINDOW_WIDTH = 800;
const uint16_t WINDOW_HEIGHT = 600;

const uint16_t DEFAULT_FONT_SIZE = 22;
const uint8_t DEFAULT_PADDING = 5;
